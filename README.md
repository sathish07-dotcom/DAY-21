In JavaScript, there are several data types that are used to store different kinds of data. 
These can be divided into 
primitive types and 
non-primitive (or reference) types.


In JavaScript, operators are used to perform operations on variables and values. 
They can be categorized into several types:
